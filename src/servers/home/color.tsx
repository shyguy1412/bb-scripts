import { ColorPicker } from "@/lib/components/ColorPicker";
import { mainWrapper } from "@/lib/WindowApp";
import React from "react";

export const main = mainWrapper(() => <ColorPicker></ColorPicker>);